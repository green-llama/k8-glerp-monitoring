apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "glerp-monitoring.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "glerp-monitoring.labels" . | nindent 4 }}
    app.kubernetes.io/component: kube-bench
